/// App-specific error types and exceptions.
///
/// Responsibilities:
/// - Define domain and data layer errors (NetworkError, NotFoundError, ParseError)
/// - Map HTTP errors to domain-friendly error types
